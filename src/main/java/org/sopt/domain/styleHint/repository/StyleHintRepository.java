package org.sopt.domain.styleHint.repository;

import org.sopt.domain.styleHint.entity.StyleHint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StyleHintRepository extends JpaRepository<StyleHint, Integer> {
}
